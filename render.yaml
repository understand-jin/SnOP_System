services:
  - type: web
    name: snop-upload
    runtime: docker
    dockerfilePath: python/Dockerfile
    dockerContext: .
    plan: free
    region: oregon
    envVars:
      - key: FIREBASE_SERVICE_ACCOUNT_JSON
        sync: false   # Render 대시보드에서 직접 입력 (보안)
    healthCheckPath: /
